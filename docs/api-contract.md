# API Contract

Preencha antes de depender de API externa: versão/base URL, dono do segredo, exemplos de entrada e saída, erros esperados, limite de uso, timeout, fixture equivalente e contrato de fallback.
